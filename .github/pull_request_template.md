- [ ] `ruff check .` and `pytest -q` pass
- [ ] hardware paths still default to dry run and go through `SafetyGovernor`
- [ ] biology claims have a reference, engineering choices are commented in config

**What this changes:**
